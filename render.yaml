services:
  - type: web
    name: brew-mud
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: python -m brewmud.web --host 0.0.0.0
    healthCheckPath: /api/status
